[package]
name = "perplexity-truth"
version = "0.1.0"
edition = "2021"

[dependencies]
rusqlite = "0.31"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# You can add more later (reqwest, clap, etc.)

[build-dependencies]
cc = "1.0"
